  %{
  #include <stdio.h>
  extern FILE* yyin;
  extern char* yytext;
  extern int yylineno;
  %}
  %token ID OP ASIGN PAR INTNR ARR FLOATNR DOUBLENR BOOLEAN CALL INT FLOAT BOOL DOUBLE 
  %start prog
  %%
  prog: declaratii bloc {printf("program corect\n");};
  bloc: ;
  declaratii: ;
  %%
  void yyerror(char * s){
  printf("eroare: %s la linia:%d\n",s,yylineno);
  }

  int main(int argc, char** argv){
  yyin=fopen(argv[1],"r");
  yyparse();
  } 
